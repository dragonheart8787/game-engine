# Online Services Contracts

- `auth/` identity and token lifecycle
- `matchmaking/` lobby/match flow
- `inventory/` player inventory/economy schema
- `liveops/` remote config, flags, events
- `cdn/` content delivery and cache invalidation
