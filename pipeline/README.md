# Pipeline

Subdomains:
- `asset-db/` GUID/dependency/version metadata
- `import/` source-to-intermediate transforms
- `build/` deterministic build graph + manifests
- `package/` distribution bundle definitions
- `patch/` diff metadata and compatibility strategy
