# Reproducible Build Requirements

- Pin toolchain versions.
- Include full build manifest with commit SHA and inputs.
- Ensure deterministic ordering of packaging entries.
- Persist cache key derivation rules.
