# Ecosystem

- `plugin-sdk/` plugin lifecycle and capability contracts
- `package-registry/` package metadata and dependency resolution
- `marketplace/` submission and review pipeline specs
- `samples/` reference extensions
