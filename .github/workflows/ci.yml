name: ci

on:
  push:
  pull_request:

jobs:
  structure-and-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check structure
        run: scripts/check-structure.sh
      - name: Lint docs footprint
        run: scripts/lint-docs.sh
